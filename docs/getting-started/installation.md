# Installation

```bash
pip install htmforge
```

Requirements

- Python 3.11+
- Pydantic v2 (declared dependency)
- MarkupSafe

Optional framework extras

```bash
pip install fastapi uvicorn
pip install flask
pip install django
```

Verify installation

```python
import htmforge
print(htmforge.__version__)
```
